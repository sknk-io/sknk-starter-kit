# AGENTS.md

This directory is a placeholder until `yarn init:contracts` initializes the contracts workspace.

Once initialized:

- `sknk-contract-creation` owns contract implementation here
- `frontend-handoff/` is reserved for machine-readable frontend bridge files
- the repo-root `game-overview.md` remains the human-readable handoff target

If `foundry.toml` is missing, the workspace is not initialized yet.
