# Frontend Handoff

This directory is reserved for machine-readable handoff files generated by `sknk-contract-creation`.

Before bootstrap it is only a placeholder.

After initialization, expected shape:

- `frontend-handoff/<Game>.json`
