# SKNK Contracts Workspace

This directory is the contracts placeholder inside `sknk-starter-kit`.

Run `yarn init:contracts` to turn it into a real Foundry workspace by importing the pinned `sknk-foundry-example` baseline in place.

After initialization, this directory becomes the target workspace for `sknk-contract-creation` and should be committed normally in your project.
