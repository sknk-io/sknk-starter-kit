# Game Overview

This file is intentionally a placeholder in the starter kit.

`sknk-contract-creation` should overwrite it with the current game's validated frontend-facing overview after contract work succeeds.

Expected downstream companions:

- `apps/contracts/frontend-handoff/<Game>.json`
- `apps/contracts/out/<Game>.sol/<Game>.json`
